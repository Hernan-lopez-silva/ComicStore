# Dependencias de ejecución — ComicStore (Django)
# Instalación: pip install -r requirements.txt
#
# Versiones compatibles con Python 3.10–3.12 (recomendado para Django 4.2).
# Si usas Python 3.13+, actualiza Django a la serie 5.x cuando lo migres.

Django>=4.2.13,<5
djangorestframework>=3.15.1,<4
whitenoise>=6.7,<7
dj-database-url>=2.2,<3
gunicorn>=22,<24

# Testing
pytest>=8.0,<9
pytest-django>=4.8,<5
pytest-cov>=5.0,<6
locust>=2.24.0,<3
